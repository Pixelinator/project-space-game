export default class PlayerController {
  constructor(inputDevice, camera) {
    this.Keyboard = inputDevice
    this.camera = camera
  }

  attachTo(model) {
    // set callbacks according to models actions in the key map
  }
}
