/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three'
import React, { useRef, useState, useEffect } from 'react'
import { useFrame } from '@react-three/fiber'
import { useGLTF } from '@react-three/drei/core/useGLTF'
import { useAnimations } from '@react-three/drei/core/useAnimations'

export default function Bee(props) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('./models/gltf/Bee.glb')
  const { actions } = useAnimations(animations, group)
  return (
    <group ref={group} {...props} dispose={null}>
      <group scale={[0.01, 0.01, 0.01]}>
        <primitive object={nodes.root_jnt2} />
        <skinnedMesh
          onClick={(e) => actions._bee_idle.play()}
          // onClick={(e) => actions._bee_hover.play()}
          // onClick={(e) => actions._bee_take_off_and_land.play()}
          material={materials.mtl_bee41}
          geometry={nodes['1645859359680_0'].geometry}
          skeleton={nodes['1645859359680_0'].skeleton}
        />
      </group>
    </group>
  )
}

useGLTF.preload('./models/gltf/Bee.glb')
